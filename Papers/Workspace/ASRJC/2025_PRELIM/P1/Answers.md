1.  
    a) 
        1) 1
        2) N-1
        3) i - 1
        4) A[j]
        5) A[j - 1]
        6) j - 1
        7) A[j + 1]
    b) The worst case time complexity is O(n^2) and it implies that the array is sorted in reverse order
    c) Time complexity of merge sort is O(n logn). Merge sort divides the array into 2 equal halves recursively and then combines both the arrays in a sorted manner by comparing each element in each array.

2. 
    a) Data integrity is the measures of a piece of data's reliability and accuracy.

3. 
    b) It is to make code more readable and to allows the reusability of code.
    c) The CPU model should be placed in all of the subclasses of the computing devices as the cpu model for each computing device is unique. The product serial number should be placed inside every child class of all the electronic device types as each product has its own unique serial number.
    d) 